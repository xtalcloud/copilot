apiVersion: v1
kind: Service
metadata:
  name: copilot-extension
  namespace: default
spec:
  selector:
    app: copilot-extension
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
